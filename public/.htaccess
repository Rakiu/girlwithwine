<IfModule mod_rewrite.c>
  RewriteEngine On

  # existing files / folders ko chhedo mat
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # React SPA fallback
  RewriteRule ^ index.html [L]
</IfModule>
